# 安全策略

## 当前支持范围

当前项目仍处于 `0.x` 阶段，只维护 `main` 分支的最新版本。旧提交、个人分支和未经确认的部署不提供安全支持。

## 安全模型

- 页面中的口令被编译进浏览器代码，只用于叙事开启效果，不是密码学意义上的秘密，也不是身份认证。
- `recipient` 模式会移除制作台入口并隔离单一实例素材，但不会提供服务端鉴权。
- `localStorage` 只用于当前浏览器的草稿和阅读进度；它没有加密，共用设备上的其他使用者可能访问这些数据。
- 真实客户项目必须使用仓库外部的 JSON 和素材目录；构建器会自动清理 `src/generated/`，交付后仍需清理明文 `dist/`。

因此，在增加服务端登录、授权、撤销和审计之前，本项目只能把公开静态托管视为虚构演示环境。

## 报告漏洞或隐私事件

请不要通过公开 Issue、Pull Request、讨论区或构建日志提交漏洞细节、真实姓名、媒体文件、访问口令或私人 URL。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。若该入口不可用，请通过仓库所有者明确提供的私人渠道联系维护者；在取得私人渠道前，仅说明“需要报告安全问题”，不要发送敏感内容。

报告应尽量包含：

- 受影响的提交或版本；
- 使用的产品模式与浏览器/运行环境；
- 仅使用虚构数据的最小复现步骤；
- 潜在影响和建议修复方向；
- 是否可能已经泄露真实数据。

维护者确认问题并完成修复前，请勿公开披露。
